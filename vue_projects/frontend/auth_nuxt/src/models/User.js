export default class User {
  constructor(id, name, email,phone, lastname, gender, token) {
    this.id = id
    this.name = name
    this.email = email
    this.phone = phone
    this.token = token
    this.lastname = lastname
    this.gender = gender
  }
}
